# Recreates the shield's socket directory at boot (/run is a tmpfs).
#   install -m 644 fips-shield-tmpfiles.conf /etc/tmpfiles.d/fips-shield.conf
# Adjust the user if nginx workers run as something other than "nginx"
# (Debian/Ubuntu packages use www-data).
d /run/fips-shield 0700 nginx nginx -
