# fips-shield: surface probing.
# Matches 444 (unknown path, connection closed without response) and
# 405 (non-GET method) in the http stage's JSON access log.

[Definition]
failregex = ^\{"ts":"[^"]*","src":"<ADDR>","status":(?:444|405),
datepattern = ^\{"ts":"{DATE}
